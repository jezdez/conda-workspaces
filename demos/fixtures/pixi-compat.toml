[workspace]
name = "my-pixi-project"
channels = ["conda-forge"]
platforms = ["osx-arm64", "linux-64"]

[dependencies]
python = ">=3.12"
numpy = "*"

[feature.test.dependencies]
pytest = "*"

[environments]
test = { features = ["test"] }
