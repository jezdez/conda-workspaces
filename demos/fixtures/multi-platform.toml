[workspace]
name = "cross-platform"
channels = ["conda-forge"]
platforms = ["linux-64", "osx-arm64"]

[dependencies]
python = ">=3.12"
requests = "*"

[feature.windows]
platforms = ["win-64"]

[environments]
default = []
windows = ["windows"]
